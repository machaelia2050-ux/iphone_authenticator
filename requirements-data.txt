# Dataset preparation dependencies
# Core libraries
numpy>=1.24.0
Pillow>=9.5.0
imagehash>=4.3.0
tqdm>=4.65.0
requests>=2.31.0

# Image filtering (optional but recommended)
opencv-python>=4.8.0

# CLIP for relevance filtering (optional - script works without it)
torch>=2.0.0
transformers>=4.30.0